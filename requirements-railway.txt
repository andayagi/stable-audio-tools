# Core FastAPI dependencies
fastapi==0.114.2
uvicorn[standard]==0.31.1

# PyTorch and ML dependencies
torch>=2.2.0
numpy>=1.23.0,<1.27.0
safetensors>=0.4.0
huggingface_hub>=0.25.0
k-diffusion>=0.1.0
tqdm>=4.66.0

# Stable Audio Tools dependencies
alias-free-torch
auraloss
descript-audio-codec
einops
einops-exts
ema-pytorch
encodec
local-attention
prefigure

# Optional: Flash Attention (will be installed separately if possible)
# flash-attn>=2.5.0
