fastapi==0.114.2
uvicorn[standard]==0.31.1

# Minimal runtime deps for inference + API
torch==2.1.0
numpy>=1.23.0,<1.27.0
safetensors>=0.4.0
huggingface_hub>=0.25.0
k-diffusion>=0.1.0
tqdm>=4.66.0

# Core dependencies from stable-audio-tools package (let pip resolve versions)
alias-free-torch
auraloss
descript-audio-codec
einops
einops-exts
ema-pytorch
encodec
local-attention
prefigure


